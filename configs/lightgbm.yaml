# LightGBM default config
features_path: data/processed/segment_year_features.parquet
model: lgbm
test_year: 2024
output_dir: models
params:
  n_estimators: 300
  learning_rate: 0.05
  max_depth: -1
  num_leaves: 64
  subsample: 0.9
  colsample_bytree: 0.9
  random_state: 42
